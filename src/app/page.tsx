"use client";
import React from 'react';
import Modal from './compornents/Modal';

const Page: React.FC = () => {
  // 他のコードやコンポーネントをここに追加

  return (
    <div>
      {/* 他のコンテンツ */}
      <Modal /> {/* Modal コンポーネントを表示 */}
    </div>
  );
};

export default Page;